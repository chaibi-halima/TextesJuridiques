{% extends '::layout.' ~ app.request.locale ~ '.html.twig' %}
{% block title %}{{ parent() }} | {{ 'Service Administratif'|trans }}{% endblock %}
{% block body %} 
    <div class="col-md-12 col-sm-12 col-xs-12">
        <div class="x_panel">
            <div class="x_title">
                <h2>{{ 'Liste des Services Administratifs'|trans }}</h2>
                <div class="btn-group pull-right">                       
                    <a class="btn btn-primary btn-xs" data-toggle="tooltip" 
                                               data-placement="top" title="" href="{{ path('service_new') }}" data-original-title="Ajouter">
                                                <span class="glyphicon glyphicon-plus"></span> {{ 'Ajouter un nouveau Service Administratif'|trans }}</a>
                </div>
                <div class="clearfix"></div>
            </div>
            <div class="x_content">
                <div class="table-responsive">
                    <table id="datatable-buttons" class="table table-striped table-bordered bulk_action">
                        <thead>
                            <tr>
                                <th>{{ 'Titre'|trans }}</th>
                                <th>{{ 'Actions'|trans }}</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for service in services %}
                                <tr>
                                    <td>{{ service.titre }}</td>
                                    <td>

                                        <div class="text-center">
                                            <a class="btn btn-primary btn-xs" data-toggle="tooltip" 
                                               data-placement="top" title="" href="{{ path('service_show', { 'id': service.id }) }}" data-original-title="afficher">
                                                <span class="glyphicon glyphicon-search"></span></a>
                                            <a class="btn btn-danger btn-xs" data-toggle="tooltip" 
                                               data-placement="top" title="" href="{{ path('service_edit', { 'id': service.id }) }}" data-original-title="modifier">
                                                <span class="glyphicon glyphicon-edit"></span></a>
                                            <a class="btn btn-danger btn-xs" data-toggle="tooltip" 
                                               data-placement="top" title="" href="{{ path('service_delete', { 'id': service.id }) }}" data-original-title="supprimer">
                                                <span class="glyphicon glyphicon-remove"></span></a>
                                        </div>

                                    </td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                    </table>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
